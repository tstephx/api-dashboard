# TODO

## Up Next

- [ ] Add MSW mock data to Storybook stories so components render without a real API
- [ ] Consider deprecating `ApiDashboard` in favor of `ApiDashboardV3` (strictly better — sparklines, histograms, pagination)
- [ ] Hook up to a real project (`book-mcp-server` or `my-mcp-portfolio`) — build a `/dashboard` endpoint and drop in the component
- [ ] v0.2.0: `AlertsPanel` component — threshold breach alerts (error rate > 5%, p95 > 500ms)
